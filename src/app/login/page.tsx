import Login from "@/components/login"

export default function LoginPage(){
    return(
        <div className="bg-gradient-to-b from-primary to-blue-dark-vs w-full min-h-screen flex justify-center items-center p-0">
            <Login/>
        </div>
    )
}